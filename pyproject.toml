# pyproject.toml

[project]
name = "arghelper"
version = "0.6.0"
description = "Python argparse helper module"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE.txt"]
authors = [{ name = "Matthew Rankin", email = "matthew@questrail.com" }]
requires-python = ">=3.9"
dependencies = []
classifiers = [
  'Programming Language :: Python',
  'Programming Language :: Python :: 3',
  'Programming Language :: Python :: 3.9',
  'Programming Language :: Python :: 3.10',
  'Programming Language :: Python :: 3.11',
  'Programming Language :: Python :: 3.12',
  'Programming Language :: Python :: 3.13',
  'Programming Language :: Python :: 3.14',
  'Development Status :: 4 - Beta',
  'Operating System :: OS Independent',
  'Intended Audience :: Developers',
  'Topic :: Software Development :: Libraries :: Python Modules',
]

[project.urls]
Homepage = "https://github.com/questrail/arghelper"
Issues = "https://github.com/questrail/arghelper/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/arghelper"]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
# Select rules explicitly rather than relying on ruff's defaults, so that the
# rule set is a deliberate choice. The ruff version itself is pinned by uv.lock.
select = [
  "E",   # pycodestyle errors
  "W",   # pycodestyle warnings
  "F",   # pyflakes
  "I",   # isort
  "UP",  # pyupgrade
  "B",   # flake8-bugbear
  "SIM", # flake8-simplify
  "EXE", # flake8-executable
  "PL",  # pylint
  "PT",  # flake8-pytest-style
  "RUF", # ruff's own rules
]

[tool.ruff.lint.per-file-ignores]
# The suite is written against unittest.TestCase, so PT objects to every
# assertion and every assertRaises in it. Converting it to plain pytest is
# worth doing and is its own job, not a side effect of choosing a rule set.
"tests/**" = ["PT009", "PT027"]

[tool.pyright]
venvPath = "."
venv = ".venv"
include = ["src", "tests"]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
    "pyright>=1.1.411",
    "ruff>=0.16.5",
]
